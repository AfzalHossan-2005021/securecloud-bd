# IMPORTANT: replace with a real secret manager (e.g., Sealed Secrets) in prod
apiVersion: v1
kind: Secret
metadata:
  name: elastic-credentials
  namespace: siem
type: Opaque
stringData:
  password: changeme   # override with: kubectl create secret generic elastic-credentials --from-literal=password=<pw>
