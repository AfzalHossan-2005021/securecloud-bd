apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: apps
  labels:
    app: payment-api
    component: bkash-demo
spec:
  selector:
    app: payment-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
